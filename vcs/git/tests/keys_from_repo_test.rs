//! `from_repo` tests — relocated from `fragmentation/tests/keys_test.rs` in T1/C3.
//!
//! TODO(T2): re-enable these once `fragmentation-git::keys::from_repo` lands as
//! the canonical free-function form. For now the binary in `src/bin/frgmt-git.rs`
//! inlines the same logic; T2 hoists it into a module these tests can target.
//!
//! Keeping this file as a placeholder so the test surface count is preserved
//! across the rename and T2 sees a clear next step.
